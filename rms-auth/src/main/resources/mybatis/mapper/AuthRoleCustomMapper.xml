<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.still.rms.auth.dao.AuthRoleCustomMapper">
  <sql id="Base_Column_List">
      <!--
        WARNING - @mbggenerated
        This element is automatically generated by MyBatis Generator, do not modify.
      -->
      id, `name`, description, user_count, create_time, `status`, sort
  </sql>
  <sql id="Where">
      <where>
          <if test="id != null">
              AND id = #{id,jdbcType=INTEGER}
          </if>
          <if test="name != null">
              AND `name` = #{name,jdbcType=VARCHAR}
          </if>
          <if test="keyword != null">
              AND (`name` like CONCAT('%',#{keyword,jdbcType=VARCHAR},'%') OR `description` like CONCAT('%',#{keyword,jdbcType=VARCHAR},'%'))
          </if>
      </where>
    </sql>

    <select id="getAuthRolesByCondition" resultType="com.still.rms.common.dto.AuthRoleDto">
        select
        <include refid="Base_Column_List"></include>
        from auth_role
        <include refid="Where"></include>
    </select>

    <select id="getAuthRoleByUserId" resultType="com.still.rms.common.dto.AuthRoleDto">
        SELECT
            r.id id,
            r.`name` `name`,
            r.description description,
            r.user_count userCount,
            r.create_time create_time,
                r.`status` `status`,
            r.sort sort
        FROM
            auth_user_role arr
                LEFT JOIN auth_role r ON arr.role_id = r.id
        WHERE
            arr.user_id = #{userId}
          AND r.id IS NOT NULL
        ORDER BY
            r.sort desc
    </select>
</mapper>