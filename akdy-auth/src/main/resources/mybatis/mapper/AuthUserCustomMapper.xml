<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.still.aikandy.auth.dao.AuthUserCustomMapper">
  <resultMap id="BaseResultMap" type="com.still.aikandy.mbg.model.AuthUser">
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="username" jdbcType="VARCHAR" property="username" />
    <result column="password" jdbcType="VARCHAR" property="password" />
    <result column="nickname" jdbcType="VARCHAR" property="nickname" />
    <result column="id_number" jdbcType="VARCHAR" property="idNumber" />
    <result column="sex" jdbcType="INTEGER" property="sex" />
    <result column="birthday" jdbcType="TIMESTAMP" property="birthday" />
    <result column="email" jdbcType="VARCHAR" property="email" />
    <result column="phone" jdbcType="VARCHAR" property="phone" />
    <result column="icon" jdbcType="VARCHAR" property="icon" />
    <result column="note" jdbcType="VARCHAR" property="note" />
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
    <result column="login_time" jdbcType="TIMESTAMP" property="loginTime" />
    <result column="status" jdbcType="INTEGER" property="status" />
  </resultMap>
  <sql id="Base_Column_List">
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    id, username, `password`, nickname, id_number, sex, birthday, email, phone, icon,
    note, create_time, login_time, `status`
  </sql>

  <sql id="Where">
    <where>
      <if test="id != null">
        AND id = #{id,jdbcType=INTEGER}
      </if>
      <if test="username != null">
        AND (`username` = #{username,jdbcType=VARCHAR})
      </if>
      <if test="nickname != null">
        AND `nickname` = #{nickname,jdbcType=VARCHAR}
      </if>
      <if test="idNumber != null">
        AND id_number = #{idNumber,jdbcType=VARCHAR}
      </if>
      <if test="sex != null">
        AND sex = #{sex,jdbcType=INTEGER}
      </if>
      <if test="birthday != null">
        AND birthday = #{birthday,jdbcType=TIMESTAMP}
      </if>
      <if test="email != null">
        AND email = #{email,jdbcType=VARCHAR}
      </if>
      <if test="phone != null">
        AND phone = #{phone,jdbcType=VARCHAR}
      </if>
      <if test="icon != null">
        AND icon = #{icon,jdbcType=VARCHAR}
      </if>
      <if test="note != null">
        AND note like CONCAT('%',#{note,jdbcType=VARCHAR},'%')
      </if>
      <if test="status != null">
        AND `status` = #{status,jdbcType=INTEGER}
      </if>
      <if test="keyword != null">
        AND (`username` like CONCAT('%',#{keyword,jdbcType=VARCHAR},'%') OR `nickname` like CONCAT('%',#{keyword,jdbcType=VARCHAR},'%') OR `note` like CONCAT('%',#{keyword,jdbcType=VARCHAR},'%'))
      </if>
    </where>
  </sql>

  <select id="getAuthUsersByCondition" resultType="com.still.aikandy.common.dto.AuthUserDto">
    select
    <include refid="Base_Column_List"></include>
    from auth_user
    <include refid="Where"></include>
  </select>

  <select id="getResourcesByUserId" resultType="com.still.aikandy.mbg.model.AuthResource">
    SELECT
        are.id id,
        are.create_time createTime,
        are.`name` `name`,
        are.url url,
        are.description description,
        are.category_id categoryId
    FROM
        auth_user_role aur
    LEFT JOIN auth_role ar ON aur.role_id = ar.id
    LEFT JOIN auth_role_resource arr ON ar.id = arr.role_id
    LEFT JOIN auth_resource are ON are.id = arr.resource_id
    WHERE
        aur.user_id = #{userId}
    AND are.id IS NOT NULL
    GROUP BY
        are.id
  </select>

</mapper>