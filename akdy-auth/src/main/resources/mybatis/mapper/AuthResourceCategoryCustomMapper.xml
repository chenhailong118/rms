<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.still.aikandy.auth.dao.AuthResourceCategoryCustomMapper">
  <sql id="Base_Column_List">
      <!--
        WARNING - @mbggenerated
        This element is automatically generated by MyBatis Generator, do not modify.
      -->
      id, `name`, sort, create_time
  </sql>
  <sql id="Where">
      <where>
          <if test="id != null">
              AND id = #{id,jdbcType=INTEGER}
          </if>
          <if test="name != null">
              AND `name` = #{name,jdbcType=VARCHAR}
          </if>
          <if test="sort != null">
              AND sort = #{sort,jdbcType=INTEGER}
          </if>
      </where>
    </sql>

  <select id="getAuthResourceCategorysByCondition" resultType="com.still.aikandy.common.dto.AuthResourceCategoryDto">
      select
      <include refid="Base_Column_List"></include>
      from auth_resource_category
      <include refid="Where"></include>
  </select>

</mapper>