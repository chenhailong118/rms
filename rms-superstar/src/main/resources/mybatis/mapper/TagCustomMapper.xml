<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.still.rms.superstar.dao.TagCustomMapper">
    <sql id="Base_Column_List">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        id, parent_id, `name`, `level`, sort, create_time, update_time, hidden
    </sql>

    <sql id="Where">
        <where>
            <if test="id != null">
                AND id = #{id,jdbcType=INTEGER}
            </if>
            <if test="parentId != null">
                AND parent_id = #{parentId,jdbcType=INTEGER}
            </if>
            <if test="name != null">
                AND name = #{name,jdbcType=VARCHAR}
            </if>
            <if test="level != null">
                AND `level` = #{level,jdbcType=INTEGER}
            </if>
            <if test="sort != null">
                AND sort = #{sort,jdbcType=INTEGER}
            </if>
            <if test="name != null">
                AND `name` = #{name,jdbcType=VARCHAR}
            </if>
            <if test="hidden != null">
                AND hidden = #{hidden,jdbcType=INTEGER}
            </if>
        </where>
    </sql>
    <select id="getTagsByCondition" resultType="com.still.rms.common.dto.TagDto">
        select
        <include refid="Base_Column_List"></include>
        from tag
        <include refid="Where"></include>
    </select>

    <select id="getTagsByResourceId" resultType="com.still.rms.common.dto.TagDto">
        select
            tag.*
        from
            tag
        left join relation_resource_tag rel
            on tag.id = rel.tag_id
        where rel.resource_id = #{resourceId}
    </select>
</mapper>